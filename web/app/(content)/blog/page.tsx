import BlogContent from "./BlogContent";

export const metadata = {
  title: "Blog, StudySwedish",
  description:
    "Sweden through a local's eyes, places, food, culture, language, and the small things that make this country unlike anywhere else.",
};

export default function BlogPage() {
  return <BlogContent />;
}
